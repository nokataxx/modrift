# v1.1 実機検証チェックリスト

v1.1 で実装した機能のうち、**シミュレータでは確認しきれず実機操作が必要な項目**。実機ビルド時に一つずつ潰す用。

> 凡例: `[ ]` = 未検証 / `[x]` = 実機で確認済み。`→` は補足・所見。

**運用**: 各機能を実装・コミットするタイミングで、その実機検証項目をこのファイルに逐次追記する（鮮度優先）。**v1.1 の開発が一通り終わったら、実機検証に入る前に一度「整理パス」を実施する**:
- 画面/フロー順に並べ替え
- 重複・陳腐化した項目を削除
- 「シミュレータ済み / 実機必須」の区分を再確認
- 横断項目の補完（ダーク・ライト両方 / 日英両方 / iPhone・iPad 両方）

## ネットワーク監視 (FR-12)

- [ ] **オフライン → オンライン復帰バナーが表示される**
      → オフライン表示（オレンジ「オフライン」）はシミュレータで確認済み。
      → **復帰（緑「オンラインに復帰しました」）はシミュレータでは検証不可** — iOS シミュレータは Wi-Fi トグル後セッション中にネットワーク復帰を認識しない既知の制約のため。実機の機内モード ON→OFF で確認する。
- [ ] オフラインバナーがホーム・ビューア両方で出る

## 設定画面 (FR-09 / FR-10)

- [x] **外観カードのサイズ感が実機で細かすぎないか** — 小さければ `src/app/settings.tsx` の `swatchOuter` の `width/height` を拡大
- [x] 文字サイズ 小 / 中 / 大 が実機で読みやすいか（小で詰まりすぎ・大で大きすぎがないか）
- [x] 外観 システム / ライト / ダーク の切り替えが実機で破綻しないか（見出しネイビー含む）
- [x] ライブプレビューが設定変更に追従して即更新されるか

## 見出しカラーリング

- [x] H1 ネイビー / H2 薄ネイビー / H3・H4 ニュートラルが実機のライト・ダーク両方で読みやすいか

## 競合警告UI (FR-13)

> **重要な前提**: 検知はローカルファイルの更新時刻に依存する。iOS はファイルを開いている間、他デバイスの変更を即座にはダウンロードしないため、「別端末で変更 → 数秒後に保存」ではほぼ検知されない（実機ログで確認済み: 保存時点では旧 mtime、数分後の再オープンで新 mtime）。これは File Provider の観測限界で仕様。**現実的な検証は「前景復帰の再チェック」経路で行う**。
>
> 再現手順（前景復帰経路）: ① 実機 Modrift でファイルを開いて1文字編集（未保存の状態を作る） → ② Modrift をバックグラウンドへ（ホームに戻る） → ③ 別デバイス（Mac の TextEdit/ターミナル等）で同じファイルを更新 → ④ 同期が落ち着くまで十数秒〜数分待つ → ⑤ Modrift に戻る（前景復帰）。

- [x] **前景復帰時に競合ダイアログが出る** — 未保存の編集がある状態で外部変更を検知 → 「ファイルが変更されています」（時間が経って同期が落ちた後に確認）
- [ ] **「編集中の内容を保持」で上書き保存できる** — 自分の編集が残り、再度開いても保持されている
- [ ] **「最新版を取得」で外部版に差し替わる** — エディタ・プレビューとも最新の内容になり、自分の編集は破棄される（編集モード中でもエディタが追従する）
- [ ] **未保存の編集が無い状態で前景復帰すると、外部変更が静かに最新へ追従する**（ダイアログは出ない）
- [ ] 競合が無い通常編集では従来どおりサイレントに自動保存される（誤検知でダイアログが出ない）
- [ ] （任意）保存時検知: 端末が既に新版を持っている状態（しばらく前に別端末で編集→同期済み）で編集→保存するとダイアログが出る

## Undo / Redo (FR-14)

> 編集モード時のみヘッダーに undo/redo アイコンが出る。実機（特に日本語 IME）での挙動確認が要。

- [x] **編集モードのヘッダーに undo/redo ボタンが出る** — プレビュー時は出ない。スタックが空なら淡色（無効）
- [x] **連続入力 → undo 1回でその塊がまとめて戻る** — 1文字ずつではない（バースト集約）
- [x] undo/redo を往復しても内容が壊れない（途中で別の編集を入れると redo がクリアされる）
- [x] **多段 undo/redo（2回目以降）が壊れない** — テキストは再マウントで差し替え、カーソルだけ後から復元する方式に変更して解消（`setNativeProps` でのテキスト差し替えは event count がズレて2回目が壊れたため不採用）
- [x] **undo/redo 後にカーソルが変更箇所に残る**（末尾に飛ばない）
- [x] **日本語 IME 入力中・変換中に undo/redo してもクラッシュ/文字化けしない**
- [x] undo した状態が 3秒後に自動保存される / 再オープンで保持される

## iCloud コピーの整理 (FR-22)

> 対象は Modrift が生成した iCloud コピー（最近リストで「iCloud」表示の行）。スワイプで「名前を変更 / 削除」が出る。

- [x] **iCloud コピー行をスワイプすると「名前を変更」「削除」が出る** — 非 iCloud 行（他アプリ/他の場所）は「削除」のみ
- [x] **リネームできる** — `Alert.prompt` で新名入力 → ファイル名が変わり、最近リストにも反映。`.md` が自動で付く
- [x] 同名が既にある場合に「同じ名前のファイルが既にあります」エラーが出る
- [x] **削除は確認ダイアログが出てから消える** — 確認後にファイル本体が消え、リストからも除去
- [x] **削除が他デバイスにも反映されるか** — iCloud Drive › Modrift から実際に消え、Mac/他端末でも消えていること（File Provider 同期）
- [x] リネーム・削除後、Files App 側（iCloud Drive › Modrift）の表示と一致しているか

## Markdown 要素のテーマ連動色

- [x] **チェックリスト（`- [x]`）のチェックON項目がダークモードで読める** — ライブラリ既定の `checkedTextColor` が黒固定で消えていた。`theme.text`（通常本文色・取り消し線なし）に修正。ライト・ダーク両方で確認
- [x] **水平線（`---`）がライトモードで見える** — ライブラリ既定が極薄グレー（`#E5E7EB`）で白背景に埋もれていた。`theme.textSecondary` に修正。ライト・ダーク両方で確認
